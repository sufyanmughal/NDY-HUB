import { PlaceholderSection } from "@/components/placeholder-page";

export default function TransactionsPage() {
  return (
    <PlaceholderSection
      title="Transactions"
      milestone="Ships in M4–M5"
      description="Full history across memberships, CRYNDY, and refunds — populated as the billing and CRYNDY pipelines come online."
    />
  );
}
