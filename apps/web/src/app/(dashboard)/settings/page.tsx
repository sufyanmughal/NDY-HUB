import { PlaceholderSection } from "@/components/placeholder-page";

export default function SettingsPage() {
  return (
    <PlaceholderSection
      title="Settings"
      milestone="Ships in M2–M3"
      description="Profile details, notification preferences, and account recovery methods."
    />
  );
}
