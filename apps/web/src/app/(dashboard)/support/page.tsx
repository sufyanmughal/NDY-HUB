import { PlaceholderSection } from "@/components/placeholder-page";

export default function SupportPage() {
  return (
    <PlaceholderSection
      title="Support"
      milestone="Not yet scheduled"
      description="Ticketing and help resources — will slot in once the admin dashboard (M6) has a queue to route requests into."
    />
  );
}
