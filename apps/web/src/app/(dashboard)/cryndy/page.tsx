import { PlaceholderSection } from "@/components/placeholder-page";

export default function CryndyPage() {
  return (
    <PlaceholderSection
      title="CRYNDY"
      milestone="Ships in M5"
      description="Purchase history, status breakdown (pending, verified, allocated, locked, available), and the Buy CRYNDY flow — once the purchase pipeline and webhook intake are built."
    />
  );
}
