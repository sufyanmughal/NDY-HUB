import { PlaceholderSection } from "@/components/placeholder-page";

export default function NdybitsPage() {
  return (
    <PlaceholderSection
      title="NDYBITS"
      milestone="Ships in M5"
      description="Balance and ledger, built alongside the CRYNDY pipeline since your mockup shows it live on the dashboard from day one."
    />
  );
}
