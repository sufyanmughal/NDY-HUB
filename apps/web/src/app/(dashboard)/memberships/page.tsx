import { PlaceholderSection } from "@/components/placeholder-page";

export default function MembershipsPage() {
  return (
    <PlaceholderSection
      title="Memberships"
      milestone="Ships in M4"
      description="Tier overview, upgrade/downgrade, billing cycle, and invoices — once Stripe subscriptions are wired in."
    />
  );
}
