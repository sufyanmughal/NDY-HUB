import { PlaceholderSection } from "@/components/placeholder-page";

export default function DocumentsPage() {
  return (
    <PlaceholderSection
      title="Documents"
      milestone="Ships in M5–M7"
      description="Invoices, CRYNDY certificates, and membership confirmations — once object storage and document generation are wired in."
    />
  );
}
